 
module.exports = {
    plugins: {
        'postcss-cssnext': {},
        //'cssnano': {   }
    }
}

// plugins: (loader) => [ 
//     //require('cssnano')({preset: 'default'}),
//     //require('postcss-nested')(),
// ],